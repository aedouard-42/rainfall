#include <stdio.h>
#include <stdlib.h>

int target = 0;

void p(char *buffer)
{
	printf(buffer);
}

void n(void)
{
	char buffer[0x208];

	fgets(buffer, 0x200, stdin);

	p(buffer);

	if (target == 0x1025544)
	{
		system("/bin/cat /home/user/level5/.pass");
	}
}

int main(void)
{
	n();
	return 0;
}